package no.ntnu.iir.idata2304.iot.apps.ingest.repository.impl;

import org.springframework.stereotype.Repository;

import lombok.RequiredArgsConstructor;

@Repository
@RequiredArgsConstructor
class CpuTemperatureMeasurementRepositoryImpl {
  
  // if we want to clean up the repositories we can use this in order
  // to hide implementation specifics.
  // a lot of the methods derived from a JpaRepository will be unused.
  // keeping this here for future reference, should be removed if we decide otherwise.

}
